import { ColorEnDosPipe } from './color-en-dos.pipe';

describe('ColorEnDosPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorEnDosPipe();
    expect(pipe).toBeTruthy();
  });
});
